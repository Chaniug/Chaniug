name: Update Recent Activity

on:
  schedule:
    - cron: "0 * * * *"  # 每小时自动运行
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: Readme-Workflows/recent-activity@main
        with:
          GH_USERNAME: Chaniug
